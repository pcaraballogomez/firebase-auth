import SwiftUI

extension LocalResources.Colors {

    struct Common {
        static let backgroundPrimary = Color("background_primary")
        static let textPrimary = Color("text_primary")
    }
}
