import Foundation

struct LocalResources {
    struct Strings {
        // struct is extended in Strings
    }
    struct Colors {
        // struct is extended in Colors
    }
    struct Images {
        // struct is extended in Images
    }
    struct Fonts {
        // struct is extended in Fonts
    }
}
