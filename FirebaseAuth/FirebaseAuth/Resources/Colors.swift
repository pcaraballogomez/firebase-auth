import SwiftUI

extension Resources.Colors {

    struct Common {
        static let backgroundPrimary = Color("background_primary")
        static let textPrimary = Color("text_primary")
    }

    struct Calendar {
        static let completed = UIColor.green
        static let half = UIColor.orange
    }
}
